var user = {
      username:"",
      password:"",
    
    isLoggedIn: function() {
      if (true) {
        console.log(this.username + " " + "is logged in.");
        };
      }
    }

    user.username = "Bob"
    console.log(user.username);
    user.isLoggedIn();

    var user = {
      username: "",
      password: "",

      isLoggedIn: function() {
        if (true) {
          console.log(this.username + " " + "is logged in.");
        };
      }
    }

    user.username = "Bob";
    console.log(user.username);
    user.isLoggedIn();

    var user = {
      username: "",
      password: "",

      isLoggedIn: function(){
        if (true) {
          console.log(this.username + " " + "is logged in.");
        };
      }
    }

    user.username= "LadyGooGoo";
    console.log(user.username);
    user.isLoggedIn();